@import com.innovenso.townplanner.model.concepts.BusinessCapability
@(capabilities: List[BusinessCapability]) @for(capability <- capabilities) {
    @plantuml.lib.concepts.txt.BusinessCapability(capability = capability)
}